#include <stdio.h>
#include <math.h>

int main ()
{
    float a, b, c, d;

    float e = 0;
    printf("input x1, y1, x2, y2 as numbers\n");

    scanf("%f %f %f %f", &a, &b, &c, &d);

    e = sqrt( ((c-a)*(c-a))*((d-b)*(d-b)) );

    printf("%.2f is a length between points " , e);

  return 0;
}
