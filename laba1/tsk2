#include <stdio.h>

int main()
{
    float a, b, c;

    printf("input a, b, c\n");
    scanf("%f %f %f", &a, &b, &c);

    if(a == b || a == c || b == c)
    {
        printf("yes");
    }
    else
    {
        printf("no");
    }

    return 0;


}
